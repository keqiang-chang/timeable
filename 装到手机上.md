# 把课表装到安卓手机上

整个过程大约 10 分钟，全部在浏览器里点，不需要写命令，不需要付费。
做完之后，手机桌面上会有一个「我的课表」图标，点开是全屏应用，**断网也能用**。

> ⚠️ **先说安全**：全程不需要把 GitHub 密码、验证码或 Token 给任何人（包括我）。
> 你只要自己登录 GitHub 网页操作就行。

---

## 第 1 步：在 GitHub 上建一个仓库

1. 手机或电脑浏览器打开 [github.com](https://github.com)，登录你的账号。
2. 右上角 **+** → **New repository**。
3. **Repository name** 填 `timetable`（名字随便，用英文）。
4. 下面选 **Public**（公开）。GitHub Pages 在免费账号下只能用于公开仓库。
5. 其他都不用勾（不用加 README、不用加 .gitignore）。
6. 点绿色按钮 **Create repository**。

---

## 第 2 步：把文件传上去

仓库建好后会看到一个空页面，点 **uploading an existing file** 这个链接
（或者页面上方的 **Add file → Upload files**）。

然后打开电脑上的文件夹：

```
D:\CODEX\课表网页
```

**把这个文件夹里的所有内容全选**（含 `icons` 文件夹），一起拖到 GitHub 网页的虚线框里。

要传的是这些：

```
index.html              ← 课表本体，必须有
styles.css              ← 样式，必须有
app.js                  ← 主程序，必须有
ai.js                   ← DeepSeek 客户端，必须有
data.js                 ← 课表数据，必须有
manifest.webmanifest    ← 应用信息，必须有
sw.js                   ← 离线缓存，必须有
apple-touch-icon.png
icons\icon-192.png
icons\icon-512.png
icons\icon-maskable-512.png
课表.ics
旁听课.ics
课程数据.json
README.md
装到手机上.md
```

> 注意：要传的是**文件夹里面的东西**，不要把「课表网页」这个文件夹本身拖进去，
> 否则 `index.html` 会跑到子目录里，网页就打不开了。
> 拖的时候 `icons` 文件夹要一起拖，它会保持目录结构。

确认文件都在列表里之后，点下方绿色的 **Commit changes**。

---

## 第 3 步：打开 GitHub Pages

1. 在仓库页面点上方 **Settings**（齿轮图标）。
2. 左侧菜单找到 **Pages**（往下翻一点）。
3. **Source** 选 **Deploy from a branch**。
4. **Branch** 选 **main**，右边的目录选 **/ (root)**，点 **Save**。
5. 等 1–2 分钟，刷新这个页面，最上面会出现一行绿底的地址，形如：

```
https://你的用户名.github.io/timetable/
```

这就是你的应用地址。先在电脑浏览器打开确认能看到课表。

---

## 第 4 步：装到手机桌面

1. 用手机上的 **Chrome** 打开上面那个地址。
2. 等页面完全加载出来。
3. 点 Chrome 右上角的 **⋮** 菜单 → **添加到主屏幕**（有的版本写「安装应用」或
   「添加到主屏幕 / Install app」）。
4. 确认名字是「我的课表」，点 **添加 / 安装**。
5. 回到桌面，会出现一个课表图标。点它打开就是全屏应用，没有地址栏。

---

## 第 5 步：验证一下真的能离线用

这一步别跳过，确认离线缓存生效了：

1. 打开桌面上的「我的课表」，确认课表正常显示。
2. 打开手机的**飞行模式**（或关掉 WiFi 和流量）。
3. 从桌面**重新点开**这个应用。
4. 课表应该照常显示。能看到就说明离线缓存成功了。
5. 关掉飞行模式。

如果第 4 步白屏，把第一条网络操作（第 4 步）重做一遍：联网打开一次，等几秒，
再断网重开。

---

## 一句话理解：日历提醒还是要单独导入

装到桌面上的这个应用，负责**看课表和课前提醒**（需要应用开着）。
但最可靠的提醒方式仍然是导入日历，**手机系统自己提醒，应用不用开着**：

把仓库里的 `课表.ics`（或 `旁听课.ics`）下载到手机，点开导入系统日历。
详细说明见 `README.md` 里的「导入日历」一节。

---

## 以后改了课表怎么更新

1. 在电脑上让我重新生成文件（改数据、加课、换教室都行）。
2. 打开 GitHub 上那个仓库，直接 **Add file → Upload files**，把改动的文件重新传一遍
   （文件名相同就会覆盖旧版本）。
3. 手机上**联网打开两次**应用，就会自动更新到最新版本，不用手动清缓存。

## 装好之后：配置 AI 功能

AI 助手和「每日」页面需要 DeepSeek 的 API Key：

1. 电脑上打开 [platform.deepseek.com](https://platform.deepseek.com)，注册登录，
   在「充值」里充点钱（最低 10 元左右）。
2. 左侧 **API Keys** → **创建 API Key** → 复制 `sk-` 开头的那串字符（**只显示一次**）。
3. 手机上打开「我的课表」→ 底部 **我的** → 粘到「DeepSeek API Key」里。
4. 点 **测试连接**，看到「连接正常」就好了。

**别忘了设置锁屏密码**：同样在「我的」页面，打开「启动时要求输入密码」，
按提示输入两遍 4-6 位数字。

---

## 可选：用命令行上传（比网页快）

如果你装了 Git，可以在电脑上打开 PowerShell：

```powershell
cd "D:\CODEX\课表网页"
git init
git add -A
git commit -m "课表应用"
git branch -M main
git remote add origin https://github.com/你的用户名/timetable.git
git push -u origin main
```

`git push` 时会让你登录。**不要输密码**——GitHub 不接受密码推送，
要去 [github.com/settings/tokens](https://github.com/settings/tokens) 生成一个
Personal Access Token（勾选 `repo` 权限）当作密码用。
用完可以随时在同一个页面删掉这个 Token。
